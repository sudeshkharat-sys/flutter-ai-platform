# Redis config for FlutterAIStudio embedded Redis
# Binds to localhost only — not exposed to network

bind 127.0.0.1
port 6379
loglevel warning
logfile ""
save ""
maxmemory 256mb
maxmemory-policy allkeys-lru
