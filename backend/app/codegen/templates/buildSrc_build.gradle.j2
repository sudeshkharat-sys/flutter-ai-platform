// buildSrc is compiled by Gradle before any project build script runs.
// Classes defined here are on the classpath for ALL build scripts in the
// project, including Kotlin DSL (.kts) files in plugin subprojects.
plugins {
    id 'groovy'
}
